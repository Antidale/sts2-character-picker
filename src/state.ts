import { reactive, ref } from 'vue';

export const Store = reactive({
    ironCladModel: ref(20),
    silentModel: ref(20),
    regentModel: ref(20),
    necrobinderModel: ref(20),
    defectModel: ref(20),
})
